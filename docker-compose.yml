services:
  gemsec:
    image: gemsec-mcp:latest
    networks:
      - proxy-net
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - proxy-net
    depends_on:
      - gemsec

networks:
  proxy-net:
    driver: bridge
